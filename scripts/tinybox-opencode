#!/bin/sh
set -eu

workspace=${1:-.}
shift || true
: "${TINYBOX_BIN:=tinybox}"
: "${TINYBOX_DAEMON:=127.0.0.1:8080}"

exec "$TINYBOX_BIN" agent launch opencode "$workspace" --daemon "$TINYBOX_DAEMON" -- "$@"
